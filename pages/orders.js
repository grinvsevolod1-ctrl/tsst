
import Header from '../components/Header'; import Footer from '../components/Footer'; import {useEffect,useState} from 'react'
export default function Orders(){ const [orders,setOrders]=useState([]); useEffect(()=>{ fetch('/api/orders').then(r=>r.json()).then(d=>setOrders(d.orders||[])) },[]); return (<div><Header /><main className="container mt-6"><div className="card max-w-3xl mx-auto"><h2 className="text-xl font-bold">История заказов</h2><div className="mt-3 space-y-3">{orders.length===0 ? <p className="text-gray-500">Заказов нет</p> : orders.map(o=>(<div key={o.id} className="border p-3 rounded"><div><b>{o.pickup} → {o.dropoff}</b></div><div className="text-sm text-gray-600">{new Date(o.createdAt).toLocaleString()}</div><div className="text-sm">Авто: {o.car} • Тел: {o.phone}</div></div>))}</div></div></main><Footer /></div>) }
