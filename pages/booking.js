
import {useState} from 'react'
export default function Booking(){ const [pickup,setPickup]=useState(''); const [drop,setDrop]=useState(''); const [phone,setPhone]=useState(''); async function submit(e){ e.preventDefault(); const r=await fetch('/api/book',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({pickup,dropoff:drop,phone,car:'economy'})}); const d=await r.json(); if (d.ok) alert('ok') else alert('err') }
  return (<div className="container"><div className="card max-w-md mx-auto mt-8"><h2>Booking</h2><form onSubmit={submit}><input value={pickup} onChange={e=>setPickup(e.target.value)} className="w-full border rounded p-2" placeholder="From" /><input value={drop} onChange={e=>setDrop(e.target.value)} className="w-full border rounded p-2 mt-2" placeholder="To" /><input value={phone} onChange={e=>setPhone(e.target.value)} className="w-full border rounded p-2 mt-2" placeholder="Phone" /><div className="mt-3"><button className="btn">Book</button></div></form></div></div>) }
